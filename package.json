{
  "name": "docs-itzemoji-com",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run src/cli.ts",
    "serve": "bun run src/serve.ts",
    "dev": "bun run build && bun run serve",
    "test": "bun test tests",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@fontsource/jetbrains-mono": "^5.3.0",
    "@fontsource/public-sans": "^5.3.0",
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
